{
  "operations": [
    {
      "name": "Aha",
      "endpoint": "/api/v1/test",
      "method": "Get",
      "queryParams": {
        "name": "string"
      },
      "query": "SELECT {name}$, {msg}$ FROM public.profile p WHERE p.name = ${name}",
      "response": {
        "name": "string",
        "msg": "string"
      }
    }
  ]
}
